apiVersion: apps/v1
kind: Deployment
metadata:
  name: gnosis-api
  namespace: gnosis-api
  labels:
    app: gnosis-api
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: gnosis-api
  template:
    metadata:
      labels:
        app: gnosis-api
    spec:
      containers:
        - name: gnosis-api
          image: ghcr.io/spearssoftware/gnosis-api:sha-47c8b95143f71f54ee0d899092664b7310e89393
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: GNOSIS_KEYS_DB_PATH
              value: /app/persist/keys.db
          envFrom:
            - secretRef:
                name: gnosis-api-secret
          volumeMounts:
            - name: keys-data
              mountPath: /app/persist
          resources:
            requests:
              memory: "512Mi"
              cpu: "100m"
            limits:
              memory: "2Gi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 5
      volumes:
        - name: keys-data
          persistentVolumeClaim:
            claimName: gnosis-api-keys
